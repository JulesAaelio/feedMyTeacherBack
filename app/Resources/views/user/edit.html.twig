{% extends 'base.html.twig' %}

{% block body %}
{#<div class="col-md-4 margin-centered">#}
    <h1>MODIFIER UN {% block type %}{% endblock %}</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        {#<input class="btn btn-default btn-lg btn-block" type="submit" value="Edit" />#}
    {{ form_end(edit_form) }}

    <div class="row justify-content-around">
        <div class="col-md-6">
            <a class="btn btn-default btn-lg btn-block btn-grey" href="{% block index_path %}{% endblock %}">Back to the list</a>
        </div>
        <div class="col-md-6">
            {{ form_start(delete_form) }}
                {{ form_widget(delete_form) }}
                {#<input class="btn btn-default btn-lg btn-block btn-red" type="submit" value="Delete">#}
            {{ form_end(delete_form) }}
        </div>
    </div>
{#</div>#}
{% endblock %}

{% extends 'base.html.twig' %}
{% block title %}MODERER UN FEEDBACK{% endblock %}
{% block body %}
    <h1>{{ block('title') }}</h1>
    <div class="col-md-6 margin-centered">
       {{ form(edit_form) }}


        <div class="row justify-content-around">
            <div class="col-md-6">
                <a class="btn btn-default btn-lg btn-block btn-grey" href="{{ path('admin_review_index') }}">RETOUR A LA LISTE</a>
            </div>
            <div class="col-md-6">
                {{ form(delete_form) }}
            </div>
        </div>
    </div>
{% endblock %}
